import React from 'react';

const Phase1Questions = ({ sessionId }) => {
    return (
        <div>
            <h1>Phase 1 Questions</h1>
            {/* Display your questions and handle user inputs */}
            <p>Your session ID: {sessionId}</p>
        </div>
    );
};

export default Phase1Questions;
